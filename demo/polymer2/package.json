{
  "name": "titanium-address-input-demo",
  "devDependencies": {
    "tslint": "*",
    "typescript": "^2.5.2",
    "bower": "^1.8.2",
    "filesystem-bower-resolver": "^1.2.0"
  },
  "scripts": {
    "postinstall": "bower install && bower update titanium-address-input",
    "lint": "tslint \"*.ts\"",
    "build": "npm run lint && tsc",
    "serve": "polymer serve"
  }
}
